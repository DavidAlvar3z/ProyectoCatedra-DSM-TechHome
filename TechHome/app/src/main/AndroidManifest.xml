<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos de Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechHome"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">

        <!-- LoginActivity - Punto de entrada de la aplicación -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OptionsActivity -->
        <activity
            android:name=".activities.OptionsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- RegisterActivity -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- WelcomeActivity - Pantalla principal después del login -->
        <activity
            android:name=".activities.WelcomeActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- ProductsActivity - Lista de productos por categoría -->
        <activity
            android:name=".activities.ProductsActivity"
            android:parentActivityName=".activities.WelcomeActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- ProductDetailActivity - Detalle del producto con stock ⭐ -->
        <activity
            android:name=".activities.ProductDetailActivity"
            android:parentActivityName=".activities.ProductsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- ProfileActivity - Perfil del usuario -->
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>

</manifest>